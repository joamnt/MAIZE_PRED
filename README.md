This repository contains the Python code used in the study to process UAV-derived vegetation indices and evaluate machine learning models for maize grain yield prediction. The scripts implement data preprocessing, model training and hyperparameter optimization, cross-validation, sub-phenotyping scenarios, cross-nitrogen prediction, and calculation of predictive performance metrics (R², RMSE, and MAE) for the evaluated regression models.
